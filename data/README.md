**Note:** This directory is for storing all project related data files. Please ensure to include the following files in this directory before running the application:

- `credentials.env`
- `api_keys.env`
- `search_engines.env`

> The files in this directory are not tracked by Git. This is intentional and prevents large files or sensitive information from being stored in the version control history.
